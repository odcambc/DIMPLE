name: dimple_gui_env
# The GUI (run_dimple_gui.py) uses tkinter, which ships with the standard
# Python distribution -- no extra GUI dependency required here.
dependencies:
  - python=3.12
  - pip
  - pip:
    - numpy==1.26.4
    - biopython==1.84
    - pydna==5.4.0
